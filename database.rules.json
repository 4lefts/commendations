{
  "rules": {
    "admins": {
      ".read": "true",
      ".write": "false"
    },
    "commendations": {
      ".read": "root.child('admins').hasChild(auth.uid)",
      "$uid":{
       	".read": "auth.uid == $uid",
       	".write": "auth.uid == $uid || root.child('admins').hasChild(auth.uid)"
      }
    }
  }
}
